import classNames from 'classnames';

import { ToIntentColorProps, toIntentColorVariables } from './toIntentColor';

export const toPlaceholderIntentColor = ({ prefix, intent = 'primary', intentWeight = 500 }: ToIntentColorProps) => {
  // Option
  const {
    isTextPrefix,
    isBGPrefix,
    isBorderPrefix,
    isOutlinePrefix,
    isRingPrefix,
    isRingOffsetPrefix,
    isFillPrefix,
    is50Weight,
    is100Weight,
    is200Weight,
    is300Weight,
    is400Weight,
    is500Weight,
    is600Weight,
    is700Weight,
    is800Weight,
    is900Weight,
    isLightIntent,
    isDarkIntent,
    isNeutralIntent,
    isPrimaryIntent,
    isInfoIntent,
    isSuccessIntent,
    isWarningIntent,
    isDangerIntent,
  } = toIntentColorVariables({
    prefix,
    intent,
    intentWeight,
  });

  return classNames({
    /**
     * @name text
     */
    // light
    'placeholder:text-light-50': isTextPrefix && is50Weight && isLightIntent,
    'placeholder:text-light-100': isTextPrefix && is100Weight && isLightIntent,
    'placeholder:text-light-200': isTextPrefix && is200Weight && isLightIntent,
    'placeholder:text-light-300': isTextPrefix && is300Weight && isLightIntent,
    'placeholder:text-light-400': isTextPrefix && is400Weight && isLightIntent,
    'placeholder:text-light-500': isTextPrefix && is500Weight && isLightIntent,
    'placeholder:text-light-600': isTextPrefix && is600Weight && isLightIntent,
    'placeholder:text-light-700': isTextPrefix && is700Weight && isLightIntent,
    'placeholder:text-light-800': isTextPrefix && is800Weight && isLightIntent,
    'placeholder:text-light-900': isTextPrefix && is900Weight && isLightIntent,
    // dark
    'placeholder:text-dark-50': isTextPrefix && is50Weight && isDarkIntent,
    'placeholder:text-dark-100': isTextPrefix && is100Weight && isDarkIntent,
    'placeholder:text-dark-200': isTextPrefix && is200Weight && isDarkIntent,
    'placeholder:text-dark-300': isTextPrefix && is300Weight && isDarkIntent,
    'placeholder:text-dark-400': isTextPrefix && is400Weight && isDarkIntent,
    'placeholder:text-dark-500': isTextPrefix && is500Weight && isDarkIntent,
    'placeholder:text-dark-600': isTextPrefix && is600Weight && isDarkIntent,
    'placeholder:text-dark-700': isTextPrefix && is700Weight && isDarkIntent,
    'placeholder:text-dark-800': isTextPrefix && is800Weight && isDarkIntent,
    'placeholder:text-dark-900': isTextPrefix && is900Weight && isDarkIntent,
    // neutral
    'placeholder:text-neutral-50': isTextPrefix && is50Weight && isNeutralIntent,
    'placeholder:text-neutral-100': isTextPrefix && is100Weight && isNeutralIntent,
    'placeholder:text-neutral-200': isTextPrefix && is200Weight && isNeutralIntent,
    'placeholder:text-neutral-300': isTextPrefix && is300Weight && isNeutralIntent,
    'placeholder:text-neutral-400': isTextPrefix && is400Weight && isNeutralIntent,
    'placeholder:text-neutral-500': isTextPrefix && is500Weight && isNeutralIntent,
    'placeholder:text-neutral-600': isTextPrefix && is600Weight && isNeutralIntent,
    'placeholder:text-neutral-700': isTextPrefix && is700Weight && isNeutralIntent,
    'placeholder:text-neutral-800': isTextPrefix && is800Weight && isNeutralIntent,
    'placeholder:text-neutral-900': isTextPrefix && is900Weight && isNeutralIntent,
    // primary
    'placeholder:text-primary-50': isTextPrefix && is50Weight && isPrimaryIntent,
    'placeholder:text-primary-100': isTextPrefix && is100Weight && isPrimaryIntent,
    'placeholder:text-primary-200': isTextPrefix && is200Weight && isPrimaryIntent,
    'placeholder:text-primary-300': isTextPrefix && is300Weight && isPrimaryIntent,
    'placeholder:text-primary-400': isTextPrefix && is400Weight && isPrimaryIntent,
    'placeholder:text-primary-500': isTextPrefix && is500Weight && isPrimaryIntent,
    'placeholder:text-primary-600': isTextPrefix && is600Weight && isPrimaryIntent,
    'placeholder:text-primary-700': isTextPrefix && is700Weight && isPrimaryIntent,
    'placeholder:text-primary-800': isTextPrefix && is800Weight && isPrimaryIntent,
    'placeholder:text-primary-900': isTextPrefix && is900Weight && isPrimaryIntent,
    // info
    'placeholder:text-info-50': isTextPrefix && is50Weight && isInfoIntent,
    'placeholder:text-info-100': isTextPrefix && is100Weight && isInfoIntent,
    'placeholder:text-info-200': isTextPrefix && is200Weight && isInfoIntent,
    'placeholder:text-info-300': isTextPrefix && is300Weight && isInfoIntent,
    'placeholder:text-info-400': isTextPrefix && is400Weight && isInfoIntent,
    'placeholder:text-info-500': isTextPrefix && is500Weight && isInfoIntent,
    'placeholder:text-info-600': isTextPrefix && is600Weight && isInfoIntent,
    'placeholder:text-info-700': isTextPrefix && is700Weight && isInfoIntent,
    'placeholder:text-info-800': isTextPrefix && is800Weight && isInfoIntent,
    'placeholder:text-info-900': isTextPrefix && is900Weight && isInfoIntent,
    // success
    'placeholder:text-success-50': isTextPrefix && is50Weight && isSuccessIntent,
    'placeholder:text-success-100': isTextPrefix && is100Weight && isSuccessIntent,
    'placeholder:text-success-200': isTextPrefix && is200Weight && isSuccessIntent,
    'placeholder:text-success-300': isTextPrefix && is300Weight && isSuccessIntent,
    'placeholder:text-success-400': isTextPrefix && is400Weight && isSuccessIntent,
    'placeholder:text-success-500': isTextPrefix && is500Weight && isSuccessIntent,
    'placeholder:text-success-600': isTextPrefix && is600Weight && isSuccessIntent,
    'placeholder:text-success-700': isTextPrefix && is700Weight && isSuccessIntent,
    'placeholder:text-success-800': isTextPrefix && is800Weight && isSuccessIntent,
    'placeholder:text-success-900': isTextPrefix && is900Weight && isSuccessIntent,
    // warning
    'placeholder:text-warning-50': isTextPrefix && is50Weight && isWarningIntent,
    'placeholder:text-warning-100': isTextPrefix && is100Weight && isWarningIntent,
    'placeholder:text-warning-200': isTextPrefix && is200Weight && isWarningIntent,
    'placeholder:text-warning-300': isTextPrefix && is300Weight && isWarningIntent,
    'placeholder:text-warning-400': isTextPrefix && is400Weight && isWarningIntent,
    'placeholder:text-warning-500': isTextPrefix && is500Weight && isWarningIntent,
    'placeholder:text-warning-600': isTextPrefix && is600Weight && isWarningIntent,
    'placeholder:text-warning-700': isTextPrefix && is700Weight && isWarningIntent,
    'placeholder:text-warning-800': isTextPrefix && is800Weight && isWarningIntent,
    'placeholder:text-warning-900': isTextPrefix && is900Weight && isWarningIntent,
    // danger
    'placeholder:text-danger-50': isTextPrefix && is50Weight && isDangerIntent,
    'placeholder:text-danger-100': isTextPrefix && is100Weight && isDangerIntent,
    'placeholder:text-danger-200': isTextPrefix && is200Weight && isDangerIntent,
    'placeholder:text-danger-300': isTextPrefix && is300Weight && isDangerIntent,
    'placeholder:text-danger-400': isTextPrefix && is400Weight && isDangerIntent,
    'placeholder:text-danger-500': isTextPrefix && is500Weight && isDangerIntent,
    'placeholder:text-danger-600': isTextPrefix && is600Weight && isDangerIntent,
    'placeholder:text-danger-700': isTextPrefix && is700Weight && isDangerIntent,
    'placeholder:text-danger-800': isTextPrefix && is800Weight && isDangerIntent,
    'placeholder:text-danger-900': isTextPrefix && is900Weight && isDangerIntent,
    /**
     * @name bg
     */
    // light
    'placeholder:bg-light-50': isBGPrefix && is50Weight && isLightIntent,
    'placeholder:bg-light-100': isBGPrefix && is100Weight && isLightIntent,
    'placeholder:bg-light-200': isBGPrefix && is200Weight && isLightIntent,
    'placeholder:bg-light-300': isBGPrefix && is300Weight && isLightIntent,
    'placeholder:bg-light-400': isBGPrefix && is400Weight && isLightIntent,
    'placeholder:bg-light-500': isBGPrefix && is500Weight && isLightIntent,
    'placeholder:bg-light-600': isBGPrefix && is600Weight && isLightIntent,
    'placeholder:bg-light-700': isBGPrefix && is700Weight && isLightIntent,
    'placeholder:bg-light-800': isBGPrefix && is800Weight && isLightIntent,
    'placeholder:bg-light-900': isBGPrefix && is900Weight && isLightIntent,
    // dark
    'placeholder:bg-dark-50': isBGPrefix && is50Weight && isDarkIntent,
    'placeholder:bg-dark-100': isBGPrefix && is100Weight && isDarkIntent,
    'placeholder:bg-dark-200': isBGPrefix && is200Weight && isDarkIntent,
    'placeholder:bg-dark-300': isBGPrefix && is300Weight && isDarkIntent,
    'placeholder:bg-dark-400': isBGPrefix && is400Weight && isDarkIntent,
    'placeholder:bg-dark-500': isBGPrefix && is500Weight && isDarkIntent,
    'placeholder:bg-dark-600': isBGPrefix && is600Weight && isDarkIntent,
    'placeholder:bg-dark-700': isBGPrefix && is700Weight && isDarkIntent,
    'placeholder:bg-dark-800': isBGPrefix && is800Weight && isDarkIntent,
    'placeholder:bg-dark-900': isBGPrefix && is900Weight && isDarkIntent,
    // neutral
    'placeholder:bg-neutral-50': isBGPrefix && is50Weight && isNeutralIntent,
    'placeholder:bg-neutral-100': isBGPrefix && is100Weight && isNeutralIntent,
    'placeholder:bg-neutral-200': isBGPrefix && is200Weight && isNeutralIntent,
    'placeholder:bg-neutral-300': isBGPrefix && is300Weight && isNeutralIntent,
    'placeholder:bg-neutral-400': isBGPrefix && is400Weight && isNeutralIntent,
    'placeholder:bg-neutral-500': isBGPrefix && is500Weight && isNeutralIntent,
    'placeholder:bg-neutral-600': isBGPrefix && is600Weight && isNeutralIntent,
    'placeholder:bg-neutral-700': isBGPrefix && is700Weight && isNeutralIntent,
    'placeholder:bg-neutral-800': isBGPrefix && is800Weight && isNeutralIntent,
    'placeholder:bg-neutral-900': isBGPrefix && is900Weight && isNeutralIntent,
    // primary
    'placeholder:bg-primary-50': isBGPrefix && is50Weight && isPrimaryIntent,
    'placeholder:bg-primary-100': isBGPrefix && is100Weight && isPrimaryIntent,
    'placeholder:bg-primary-200': isBGPrefix && is200Weight && isPrimaryIntent,
    'placeholder:bg-primary-300': isBGPrefix && is300Weight && isPrimaryIntent,
    'placeholder:bg-primary-400': isBGPrefix && is400Weight && isPrimaryIntent,
    'placeholder:bg-primary-500': isBGPrefix && is500Weight && isPrimaryIntent,
    'placeholder:bg-primary-600': isBGPrefix && is600Weight && isPrimaryIntent,
    'placeholder:bg-primary-700': isBGPrefix && is700Weight && isPrimaryIntent,
    'placeholder:bg-primary-800': isBGPrefix && is800Weight && isPrimaryIntent,
    'placeholder:bg-primary-900': isBGPrefix && is900Weight && isPrimaryIntent,
    // info
    'placeholder:bg-info-50': isBGPrefix && is50Weight && isInfoIntent,
    'placeholder:bg-info-100': isBGPrefix && is100Weight && isInfoIntent,
    'placeholder:bg-info-200': isBGPrefix && is200Weight && isInfoIntent,
    'placeholder:bg-info-300': isBGPrefix && is300Weight && isInfoIntent,
    'placeholder:bg-info-400': isBGPrefix && is400Weight && isInfoIntent,
    'placeholder:bg-info-500': isBGPrefix && is500Weight && isInfoIntent,
    'placeholder:bg-info-600': isBGPrefix && is600Weight && isInfoIntent,
    'placeholder:bg-info-700': isBGPrefix && is700Weight && isInfoIntent,
    'placeholder:bg-info-800': isBGPrefix && is800Weight && isInfoIntent,
    'placeholder:bg-info-900': isBGPrefix && is900Weight && isInfoIntent,
    // success
    'placeholder:bg-success-50': isBGPrefix && is50Weight && isSuccessIntent,
    'placeholder:bg-success-100': isBGPrefix && is100Weight && isSuccessIntent,
    'placeholder:bg-success-200': isBGPrefix && is200Weight && isSuccessIntent,
    'placeholder:bg-success-300': isBGPrefix && is300Weight && isSuccessIntent,
    'placeholder:bg-success-400': isBGPrefix && is400Weight && isSuccessIntent,
    'placeholder:bg-success-500': isBGPrefix && is500Weight && isSuccessIntent,
    'placeholder:bg-success-600': isBGPrefix && is600Weight && isSuccessIntent,
    'placeholder:bg-success-700': isBGPrefix && is700Weight && isSuccessIntent,
    'placeholder:bg-success-800': isBGPrefix && is800Weight && isSuccessIntent,
    'placeholder:bg-success-900': isBGPrefix && is900Weight && isSuccessIntent,
    // warning
    'placeholder:bg-warning-50': isBGPrefix && is50Weight && isWarningIntent,
    'placeholder:bg-warning-100': isBGPrefix && is100Weight && isWarningIntent,
    'placeholder:bg-warning-200': isBGPrefix && is200Weight && isWarningIntent,
    'placeholder:bg-warning-300': isBGPrefix && is300Weight && isWarningIntent,
    'placeholder:bg-warning-400': isBGPrefix && is400Weight && isWarningIntent,
    'placeholder:bg-warning-500': isBGPrefix && is500Weight && isWarningIntent,
    'placeholder:bg-warning-600': isBGPrefix && is600Weight && isWarningIntent,
    'placeholder:bg-warning-700': isBGPrefix && is700Weight && isWarningIntent,
    'placeholder:bg-warning-800': isBGPrefix && is800Weight && isWarningIntent,
    'placeholder:bg-warning-900': isBGPrefix && is900Weight && isWarningIntent,
    // danger
    'placeholder:bg-danger-50': isBGPrefix && is50Weight && isDangerIntent,
    'placeholder:bg-danger-100': isBGPrefix && is100Weight && isDangerIntent,
    'placeholder:bg-danger-200': isBGPrefix && is200Weight && isDangerIntent,
    'placeholder:bg-danger-300': isBGPrefix && is300Weight && isDangerIntent,
    'placeholder:bg-danger-400': isBGPrefix && is400Weight && isDangerIntent,
    'placeholder:bg-danger-500': isBGPrefix && is500Weight && isDangerIntent,
    'placeholder:bg-danger-600': isBGPrefix && is600Weight && isDangerIntent,
    'placeholder:bg-danger-700': isBGPrefix && is700Weight && isDangerIntent,
    'placeholder:bg-danger-800': isBGPrefix && is800Weight && isDangerIntent,
    'placeholder:bg-danger-900': isBGPrefix && is900Weight && isDangerIntent,
    /**
     * @name border
     */
    // light
    'placeholder:border-light-50': isBorderPrefix && is50Weight && isLightIntent,
    'placeholder:border-light-100': isBorderPrefix && is100Weight && isLightIntent,
    'placeholder:border-light-200': isBorderPrefix && is200Weight && isLightIntent,
    'placeholder:border-light-300': isBorderPrefix && is300Weight && isLightIntent,
    'placeholder:border-light-400': isBorderPrefix && is400Weight && isLightIntent,
    'placeholder:border-light-500': isBorderPrefix && is500Weight && isLightIntent,
    'placeholder:border-light-600': isBorderPrefix && is600Weight && isLightIntent,
    'placeholder:border-light-700': isBorderPrefix && is700Weight && isLightIntent,
    'placeholder:border-light-800': isBorderPrefix && is800Weight && isLightIntent,
    'placeholder:border-light-900': isBorderPrefix && is900Weight && isLightIntent,
    // dark
    'placeholder:border-dark-50': isBorderPrefix && is50Weight && isDarkIntent,
    'placeholder:border-dark-100': isBorderPrefix && is100Weight && isDarkIntent,
    'placeholder:border-dark-200': isBorderPrefix && is200Weight && isDarkIntent,
    'placeholder:border-dark-300': isBorderPrefix && is300Weight && isDarkIntent,
    'placeholder:border-dark-400': isBorderPrefix && is400Weight && isDarkIntent,
    'placeholder:border-dark-500': isBorderPrefix && is500Weight && isDarkIntent,
    'placeholder:border-dark-600': isBorderPrefix && is600Weight && isDarkIntent,
    'placeholder:border-dark-700': isBorderPrefix && is700Weight && isDarkIntent,
    'placeholder:border-dark-800': isBorderPrefix && is800Weight && isDarkIntent,
    'placeholder:border-dark-900': isBorderPrefix && is900Weight && isDarkIntent,
    // neutral
    'placeholder:border-neutral-50': isBorderPrefix && is50Weight && isNeutralIntent,
    'placeholder:border-neutral-100': isBorderPrefix && is100Weight && isNeutralIntent,
    'placeholder:border-neutral-200': isBorderPrefix && is200Weight && isNeutralIntent,
    'placeholder:border-neutral-300': isBorderPrefix && is300Weight && isNeutralIntent,
    'placeholder:border-neutral-400': isBorderPrefix && is400Weight && isNeutralIntent,
    'placeholder:border-neutral-500': isBorderPrefix && is500Weight && isNeutralIntent,
    'placeholder:border-neutral-600': isBorderPrefix && is600Weight && isNeutralIntent,
    'placeholder:border-neutral-700': isBorderPrefix && is700Weight && isNeutralIntent,
    'placeholder:border-neutral-800': isBorderPrefix && is800Weight && isNeutralIntent,
    'placeholder:border-neutral-900': isBorderPrefix && is900Weight && isNeutralIntent,
    // primary
    'placeholder:border-primary-50': isBorderPrefix && is50Weight && isPrimaryIntent,
    'placeholder:border-primary-100': isBorderPrefix && is100Weight && isPrimaryIntent,
    'placeholder:border-primary-200': isBorderPrefix && is200Weight && isPrimaryIntent,
    'placeholder:border-primary-300': isBorderPrefix && is300Weight && isPrimaryIntent,
    'placeholder:border-primary-400': isBorderPrefix && is400Weight && isPrimaryIntent,
    'placeholder:border-primary-500': isBorderPrefix && is500Weight && isPrimaryIntent,
    'placeholder:border-primary-600': isBorderPrefix && is600Weight && isPrimaryIntent,
    'placeholder:border-primary-700': isBorderPrefix && is700Weight && isPrimaryIntent,
    'placeholder:border-primary-800': isBorderPrefix && is800Weight && isPrimaryIntent,
    'placeholder:border-primary-900': isBorderPrefix && is900Weight && isPrimaryIntent,
    // info
    'placeholder:border-info-50': isBorderPrefix && is50Weight && isInfoIntent,
    'placeholder:border-info-100': isBorderPrefix && is100Weight && isInfoIntent,
    'placeholder:border-info-200': isBorderPrefix && is200Weight && isInfoIntent,
    'placeholder:border-info-300': isBorderPrefix && is300Weight && isInfoIntent,
    'placeholder:border-info-400': isBorderPrefix && is400Weight && isInfoIntent,
    'placeholder:border-info-500': isBorderPrefix && is500Weight && isInfoIntent,
    'placeholder:border-info-600': isBorderPrefix && is600Weight && isInfoIntent,
    'placeholder:border-info-700': isBorderPrefix && is700Weight && isInfoIntent,
    'placeholder:border-info-800': isBorderPrefix && is800Weight && isInfoIntent,
    'placeholder:border-info-900': isBorderPrefix && is900Weight && isInfoIntent,
    // success
    'placeholder:border-success-50': isBorderPrefix && is50Weight && isSuccessIntent,
    'placeholder:border-success-100': isBorderPrefix && is100Weight && isSuccessIntent,
    'placeholder:border-success-200': isBorderPrefix && is200Weight && isSuccessIntent,
    'placeholder:border-success-300': isBorderPrefix && is300Weight && isSuccessIntent,
    'placeholder:border-success-400': isBorderPrefix && is400Weight && isSuccessIntent,
    'placeholder:border-success-500': isBorderPrefix && is500Weight && isSuccessIntent,
    'placeholder:border-success-600': isBorderPrefix && is600Weight && isSuccessIntent,
    'placeholder:border-success-700': isBorderPrefix && is700Weight && isSuccessIntent,
    'placeholder:border-success-800': isBorderPrefix && is800Weight && isSuccessIntent,
    'placeholder:border-success-900': isBorderPrefix && is900Weight && isSuccessIntent,
    // warning
    'placeholder:border-warning-50': isBorderPrefix && is50Weight && isWarningIntent,
    'placeholder:border-warning-100': isBorderPrefix && is100Weight && isWarningIntent,
    'placeholder:border-warning-200': isBorderPrefix && is200Weight && isWarningIntent,
    'placeholder:border-warning-300': isBorderPrefix && is300Weight && isWarningIntent,
    'placeholder:border-warning-400': isBorderPrefix && is400Weight && isWarningIntent,
    'placeholder:border-warning-500': isBorderPrefix && is500Weight && isWarningIntent,
    'placeholder:border-warning-600': isBorderPrefix && is600Weight && isWarningIntent,
    'placeholder:border-warning-700': isBorderPrefix && is700Weight && isWarningIntent,
    'placeholder:border-warning-800': isBorderPrefix && is800Weight && isWarningIntent,
    'placeholder:border-warning-900': isBorderPrefix && is900Weight && isWarningIntent,
    // danger
    'placeholder:border-danger-50': isBorderPrefix && is50Weight && isDangerIntent,
    'placeholder:border-danger-100': isBorderPrefix && is100Weight && isDangerIntent,
    'placeholder:border-danger-200': isBorderPrefix && is200Weight && isDangerIntent,
    'placeholder:border-danger-300': isBorderPrefix && is300Weight && isDangerIntent,
    'placeholder:border-danger-400': isBorderPrefix && is400Weight && isDangerIntent,
    'placeholder:border-danger-500': isBorderPrefix && is500Weight && isDangerIntent,
    'placeholder:border-danger-600': isBorderPrefix && is600Weight && isDangerIntent,
    'placeholder:border-danger-700': isBorderPrefix && is700Weight && isDangerIntent,
    'placeholder:border-danger-800': isBorderPrefix && is800Weight && isDangerIntent,
    'placeholder:border-danger-900': isBorderPrefix && is900Weight && isDangerIntent,
    /**
     * @name outline
     */
    // light
    'placeholder:outline-light-50': isOutlinePrefix && is50Weight && isLightIntent,
    'placeholder:outline-light-100': isOutlinePrefix && is100Weight && isLightIntent,
    'placeholder:outline-light-200': isOutlinePrefix && is200Weight && isLightIntent,
    'placeholder:outline-light-300': isOutlinePrefix && is300Weight && isLightIntent,
    'placeholder:outline-light-400': isOutlinePrefix && is400Weight && isLightIntent,
    'placeholder:outline-light-500': isOutlinePrefix && is500Weight && isLightIntent,
    'placeholder:outline-light-600': isOutlinePrefix && is600Weight && isLightIntent,
    'placeholder:outline-light-700': isOutlinePrefix && is700Weight && isLightIntent,
    'placeholder:outline-light-800': isOutlinePrefix && is800Weight && isLightIntent,
    'placeholder:outline-light-900': isOutlinePrefix && is900Weight && isLightIntent,
    // dark
    'placeholder:outline-dark-50': isOutlinePrefix && is50Weight && isDarkIntent,
    'placeholder:outline-dark-100': isOutlinePrefix && is100Weight && isDarkIntent,
    'placeholder:outline-dark-200': isOutlinePrefix && is200Weight && isDarkIntent,
    'placeholder:outline-dark-300': isOutlinePrefix && is300Weight && isDarkIntent,
    'placeholder:outline-dark-400': isOutlinePrefix && is400Weight && isDarkIntent,
    'placeholder:outline-dark-500': isOutlinePrefix && is500Weight && isDarkIntent,
    'placeholder:outline-dark-600': isOutlinePrefix && is600Weight && isDarkIntent,
    'placeholder:outline-dark-700': isOutlinePrefix && is700Weight && isDarkIntent,
    'placeholder:outline-dark-800': isOutlinePrefix && is800Weight && isDarkIntent,
    'placeholder:outline-dark-900': isOutlinePrefix && is900Weight && isDarkIntent,
    // neutral
    'placeholder:outline-neutral-50': isOutlinePrefix && is50Weight && isNeutralIntent,
    'placeholder:outline-neutral-100': isOutlinePrefix && is100Weight && isNeutralIntent,
    'placeholder:outline-neutral-200': isOutlinePrefix && is200Weight && isNeutralIntent,
    'placeholder:outline-neutral-300': isOutlinePrefix && is300Weight && isNeutralIntent,
    'placeholder:outline-neutral-400': isOutlinePrefix && is400Weight && isNeutralIntent,
    'placeholder:outline-neutral-500': isOutlinePrefix && is500Weight && isNeutralIntent,
    'placeholder:outline-neutral-600': isOutlinePrefix && is600Weight && isNeutralIntent,
    'placeholder:outline-neutral-700': isOutlinePrefix && is700Weight && isNeutralIntent,
    'placeholder:outline-neutral-800': isOutlinePrefix && is800Weight && isNeutralIntent,
    'placeholder:outline-neutral-900': isOutlinePrefix && is900Weight && isNeutralIntent,
    // primary
    'placeholder:outline-primary-50': isOutlinePrefix && is50Weight && isPrimaryIntent,
    'placeholder:outline-primary-100': isOutlinePrefix && is100Weight && isPrimaryIntent,
    'placeholder:outline-primary-200': isOutlinePrefix && is200Weight && isPrimaryIntent,
    'placeholder:outline-primary-300': isOutlinePrefix && is300Weight && isPrimaryIntent,
    'placeholder:outline-primary-400': isOutlinePrefix && is400Weight && isPrimaryIntent,
    'placeholder:outline-primary-500': isOutlinePrefix && is500Weight && isPrimaryIntent,
    'placeholder:outline-primary-600': isOutlinePrefix && is600Weight && isPrimaryIntent,
    'placeholder:outline-primary-700': isOutlinePrefix && is700Weight && isPrimaryIntent,
    'placeholder:outline-primary-800': isOutlinePrefix && is800Weight && isPrimaryIntent,
    'placeholder:outline-primary-900': isOutlinePrefix && is900Weight && isPrimaryIntent,
    // info
    'placeholder:outline-info-50': isOutlinePrefix && is50Weight && isInfoIntent,
    'placeholder:outline-info-100': isOutlinePrefix && is100Weight && isInfoIntent,
    'placeholder:outline-info-200': isOutlinePrefix && is200Weight && isInfoIntent,
    'placeholder:outline-info-300': isOutlinePrefix && is300Weight && isInfoIntent,
    'placeholder:outline-info-400': isOutlinePrefix && is400Weight && isInfoIntent,
    'placeholder:outline-info-500': isOutlinePrefix && is500Weight && isInfoIntent,
    'placeholder:outline-info-600': isOutlinePrefix && is600Weight && isInfoIntent,
    'placeholder:outline-info-700': isOutlinePrefix && is700Weight && isInfoIntent,
    'placeholder:outline-info-800': isOutlinePrefix && is800Weight && isInfoIntent,
    'placeholder:outline-info-900': isOutlinePrefix && is900Weight && isInfoIntent,
    // success
    'placeholder:outline-success-50': isOutlinePrefix && is50Weight && isSuccessIntent,
    'placeholder:outline-success-100': isOutlinePrefix && is100Weight && isSuccessIntent,
    'placeholder:outline-success-200': isOutlinePrefix && is200Weight && isSuccessIntent,
    'placeholder:outline-success-300': isOutlinePrefix && is300Weight && isSuccessIntent,
    'placeholder:outline-success-400': isOutlinePrefix && is400Weight && isSuccessIntent,
    'placeholder:outline-success-500': isOutlinePrefix && is500Weight && isSuccessIntent,
    'placeholder:outline-success-600': isOutlinePrefix && is600Weight && isSuccessIntent,
    'placeholder:outline-success-700': isOutlinePrefix && is700Weight && isSuccessIntent,
    'placeholder:outline-success-800': isOutlinePrefix && is800Weight && isSuccessIntent,
    'placeholder:outline-success-900': isOutlinePrefix && is900Weight && isSuccessIntent,
    // warning
    'placeholder:outline-warning-50': isOutlinePrefix && is50Weight && isWarningIntent,
    'placeholder:outline-warning-100': isOutlinePrefix && is100Weight && isWarningIntent,
    'placeholder:outline-warning-200': isOutlinePrefix && is200Weight && isWarningIntent,
    'placeholder:outline-warning-300': isOutlinePrefix && is300Weight && isWarningIntent,
    'placeholder:outline-warning-400': isOutlinePrefix && is400Weight && isWarningIntent,
    'placeholder:outline-warning-500': isOutlinePrefix && is500Weight && isWarningIntent,
    'placeholder:outline-warning-600': isOutlinePrefix && is600Weight && isWarningIntent,
    'placeholder:outline-warning-700': isOutlinePrefix && is700Weight && isWarningIntent,
    'placeholder:outline-warning-800': isOutlinePrefix && is800Weight && isWarningIntent,
    'placeholder:outline-warning-900': isOutlinePrefix && is900Weight && isWarningIntent,
    // danger
    'placeholder:outline-danger-50': isOutlinePrefix && is50Weight && isDangerIntent,
    'placeholder:outline-danger-100': isOutlinePrefix && is100Weight && isDangerIntent,
    'placeholder:outline-danger-200': isOutlinePrefix && is200Weight && isDangerIntent,
    'placeholder:outline-danger-300': isOutlinePrefix && is300Weight && isDangerIntent,
    'placeholder:outline-danger-400': isOutlinePrefix && is400Weight && isDangerIntent,
    'placeholder:outline-danger-500': isOutlinePrefix && is500Weight && isDangerIntent,
    'placeholder:outline-danger-600': isOutlinePrefix && is600Weight && isDangerIntent,
    'placeholder:outline-danger-700': isOutlinePrefix && is700Weight && isDangerIntent,
    'placeholder:outline-danger-800': isOutlinePrefix && is800Weight && isDangerIntent,
    'placeholder:outline-danger-900': isOutlinePrefix && is900Weight && isDangerIntent,
    /**
     * @name ring
     */
    // light
    'placeholder:ring-light-50': isRingPrefix && is50Weight && isLightIntent,
    'placeholder:ring-light-100': isRingPrefix && is100Weight && isLightIntent,
    'placeholder:ring-light-200': isRingPrefix && is200Weight && isLightIntent,
    'placeholder:ring-light-300': isRingPrefix && is300Weight && isLightIntent,
    'placeholder:ring-light-400': isRingPrefix && is400Weight && isLightIntent,
    'placeholder:ring-light-500': isRingPrefix && is500Weight && isLightIntent,
    'placeholder:ring-light-600': isRingPrefix && is600Weight && isLightIntent,
    'placeholder:ring-light-700': isRingPrefix && is700Weight && isLightIntent,
    'placeholder:ring-light-800': isRingPrefix && is800Weight && isLightIntent,
    'placeholder:ring-light-900': isRingPrefix && is900Weight && isLightIntent,
    // dark
    'placeholder:ring-dark-50': isRingPrefix && is50Weight && isDarkIntent,
    'placeholder:ring-dark-100': isRingPrefix && is100Weight && isDarkIntent,
    'placeholder:ring-dark-200': isRingPrefix && is200Weight && isDarkIntent,
    'placeholder:ring-dark-300': isRingPrefix && is300Weight && isDarkIntent,
    'placeholder:ring-dark-400': isRingPrefix && is400Weight && isDarkIntent,
    'placeholder:ring-dark-500': isRingPrefix && is500Weight && isDarkIntent,
    'placeholder:ring-dark-600': isRingPrefix && is600Weight && isDarkIntent,
    'placeholder:ring-dark-700': isRingPrefix && is700Weight && isDarkIntent,
    'placeholder:ring-dark-800': isRingPrefix && is800Weight && isDarkIntent,
    'placeholder:ring-dark-900': isRingPrefix && is900Weight && isDarkIntent,
    // neutral
    'placeholder:ring-neutral-50': isRingPrefix && is50Weight && isNeutralIntent,
    'placeholder:ring-neutral-100': isRingPrefix && is100Weight && isNeutralIntent,
    'placeholder:ring-neutral-200': isRingPrefix && is200Weight && isNeutralIntent,
    'placeholder:ring-neutral-300': isRingPrefix && is300Weight && isNeutralIntent,
    'placeholder:ring-neutral-400': isRingPrefix && is400Weight && isNeutralIntent,
    'placeholder:ring-neutral-500': isRingPrefix && is500Weight && isNeutralIntent,
    'placeholder:ring-neutral-600': isRingPrefix && is600Weight && isNeutralIntent,
    'placeholder:ring-neutral-700': isRingPrefix && is700Weight && isNeutralIntent,
    'placeholder:ring-neutral-800': isRingPrefix && is800Weight && isNeutralIntent,
    'placeholder:ring-neutral-900': isRingPrefix && is900Weight && isNeutralIntent,
    // primary
    'placeholder:ring-primary-50': isRingPrefix && is50Weight && isPrimaryIntent,
    'placeholder:ring-primary-100': isRingPrefix && is100Weight && isPrimaryIntent,
    'placeholder:ring-primary-200': isRingPrefix && is200Weight && isPrimaryIntent,
    'placeholder:ring-primary-300': isRingPrefix && is300Weight && isPrimaryIntent,
    'placeholder:ring-primary-400': isRingPrefix && is400Weight && isPrimaryIntent,
    'placeholder:ring-primary-500': isRingPrefix && is500Weight && isPrimaryIntent,
    'placeholder:ring-primary-600': isRingPrefix && is600Weight && isPrimaryIntent,
    'placeholder:ring-primary-700': isRingPrefix && is700Weight && isPrimaryIntent,
    'placeholder:ring-primary-800': isRingPrefix && is800Weight && isPrimaryIntent,
    'placeholder:ring-primary-900': isRingPrefix && is900Weight && isPrimaryIntent,
    // info
    'placeholder:ring-info-50': isRingPrefix && is50Weight && isInfoIntent,
    'placeholder:ring-info-100': isRingPrefix && is100Weight && isInfoIntent,
    'placeholder:ring-info-200': isRingPrefix && is200Weight && isInfoIntent,
    'placeholder:ring-info-300': isRingPrefix && is300Weight && isInfoIntent,
    'placeholder:ring-info-400': isRingPrefix && is400Weight && isInfoIntent,
    'placeholder:ring-info-500': isRingPrefix && is500Weight && isInfoIntent,
    'placeholder:ring-info-600': isRingPrefix && is600Weight && isInfoIntent,
    'placeholder:ring-info-700': isRingPrefix && is700Weight && isInfoIntent,
    'placeholder:ring-info-800': isRingPrefix && is800Weight && isInfoIntent,
    'placeholder:ring-info-900': isRingPrefix && is900Weight && isInfoIntent,
    // success
    'placeholder:ring-success-50': isRingPrefix && is50Weight && isSuccessIntent,
    'placeholder:ring-success-100': isRingPrefix && is100Weight && isSuccessIntent,
    'placeholder:ring-success-200': isRingPrefix && is200Weight && isSuccessIntent,
    'placeholder:ring-success-300': isRingPrefix && is300Weight && isSuccessIntent,
    'placeholder:ring-success-400': isRingPrefix && is400Weight && isSuccessIntent,
    'placeholder:ring-success-500': isRingPrefix && is500Weight && isSuccessIntent,
    'placeholder:ring-success-600': isRingPrefix && is600Weight && isSuccessIntent,
    'placeholder:ring-success-700': isRingPrefix && is700Weight && isSuccessIntent,
    'placeholder:ring-success-800': isRingPrefix && is800Weight && isSuccessIntent,
    'placeholder:ring-success-900': isRingPrefix && is900Weight && isSuccessIntent,
    // warning
    'placeholder:ring-warning-50': isRingPrefix && is50Weight && isWarningIntent,
    'placeholder:ring-warning-100': isRingPrefix && is100Weight && isWarningIntent,
    'placeholder:ring-warning-200': isRingPrefix && is200Weight && isWarningIntent,
    'placeholder:ring-warning-300': isRingPrefix && is300Weight && isWarningIntent,
    'placeholder:ring-warning-400': isRingPrefix && is400Weight && isWarningIntent,
    'placeholder:ring-warning-500': isRingPrefix && is500Weight && isWarningIntent,
    'placeholder:ring-warning-600': isRingPrefix && is600Weight && isWarningIntent,
    'placeholder:ring-warning-700': isRingPrefix && is700Weight && isWarningIntent,
    'placeholder:ring-warning-800': isRingPrefix && is800Weight && isWarningIntent,
    'placeholder:ring-warning-900': isRingPrefix && is900Weight && isWarningIntent,
    // danger
    'placeholder:ring-danger-50': isRingPrefix && is50Weight && isDangerIntent,
    'placeholder:ring-danger-100': isRingPrefix && is100Weight && isDangerIntent,
    'placeholder:ring-danger-200': isRingPrefix && is200Weight && isDangerIntent,
    'placeholder:ring-danger-300': isRingPrefix && is300Weight && isDangerIntent,
    'placeholder:ring-danger-400': isRingPrefix && is400Weight && isDangerIntent,
    'placeholder:ring-danger-500': isRingPrefix && is500Weight && isDangerIntent,
    'placeholder:ring-danger-600': isRingPrefix && is600Weight && isDangerIntent,
    'placeholder:ring-danger-700': isRingPrefix && is700Weight && isDangerIntent,
    'placeholder:ring-danger-800': isRingPrefix && is800Weight && isDangerIntent,
    'placeholder:ring-danger-900': isRingPrefix && is900Weight && isDangerIntent,
    /**
     * @name ring-offset
     */
    // light
    'placeholder:ring-offset-light-50': isRingOffsetPrefix && is50Weight && isLightIntent,
    'placeholder:ring-offset-light-100': isRingOffsetPrefix && is100Weight && isLightIntent,
    'placeholder:ring-offset-light-200': isRingOffsetPrefix && is200Weight && isLightIntent,
    'placeholder:ring-offset-light-300': isRingOffsetPrefix && is300Weight && isLightIntent,
    'placeholder:ring-offset-light-400': isRingOffsetPrefix && is400Weight && isLightIntent,
    'placeholder:ring-offset-light-500': isRingOffsetPrefix && is500Weight && isLightIntent,
    'placeholder:ring-offset-light-600': isRingOffsetPrefix && is600Weight && isLightIntent,
    'placeholder:ring-offset-light-700': isRingOffsetPrefix && is700Weight && isLightIntent,
    'placeholder:ring-offset-light-800': isRingOffsetPrefix && is800Weight && isLightIntent,
    'placeholder:ring-offset-light-900': isRingOffsetPrefix && is900Weight && isLightIntent,
    // dark
    'placeholder:ring-offset-dark-50': isRingOffsetPrefix && is50Weight && isDarkIntent,
    'placeholder:ring-offset-dark-100': isRingOffsetPrefix && is100Weight && isDarkIntent,
    'placeholder:ring-offset-dark-200': isRingOffsetPrefix && is200Weight && isDarkIntent,
    'placeholder:ring-offset-dark-300': isRingOffsetPrefix && is300Weight && isDarkIntent,
    'placeholder:ring-offset-dark-400': isRingOffsetPrefix && is400Weight && isDarkIntent,
    'placeholder:ring-offset-dark-500': isRingOffsetPrefix && is500Weight && isDarkIntent,
    'placeholder:ring-offset-dark-600': isRingOffsetPrefix && is600Weight && isDarkIntent,
    'placeholder:ring-offset-dark-700': isRingOffsetPrefix && is700Weight && isDarkIntent,
    'placeholder:ring-offset-dark-800': isRingOffsetPrefix && is800Weight && isDarkIntent,
    'placeholder:ring-offset-dark-900': isRingOffsetPrefix && is900Weight && isDarkIntent,
    // neutral
    'placeholder:ring-offset-neutral-50': isRingOffsetPrefix && is50Weight && isNeutralIntent,
    'placeholder:ring-offset-neutral-100': isRingOffsetPrefix && is100Weight && isNeutralIntent,
    'placeholder:ring-offset-neutral-200': isRingOffsetPrefix && is200Weight && isNeutralIntent,
    'placeholder:ring-offset-neutral-300': isRingOffsetPrefix && is300Weight && isNeutralIntent,
    'placeholder:ring-offset-neutral-400': isRingOffsetPrefix && is400Weight && isNeutralIntent,
    'placeholder:ring-offset-neutral-500': isRingOffsetPrefix && is500Weight && isNeutralIntent,
    'placeholder:ring-offset-neutral-600': isRingOffsetPrefix && is600Weight && isNeutralIntent,
    'placeholder:ring-offset-neutral-700': isRingOffsetPrefix && is700Weight && isNeutralIntent,
    'placeholder:ring-offset-neutral-800': isRingOffsetPrefix && is800Weight && isNeutralIntent,
    'placeholder:ring-offset-neutral-900': isRingOffsetPrefix && is900Weight && isNeutralIntent,
    // primary
    'placeholder:ring-offset-primary-50': isRingOffsetPrefix && is50Weight && isPrimaryIntent,
    'placeholder:ring-offset-primary-100': isRingOffsetPrefix && is100Weight && isPrimaryIntent,
    'placeholder:ring-offset-primary-200': isRingOffsetPrefix && is200Weight && isPrimaryIntent,
    'placeholder:ring-offset-primary-300': isRingOffsetPrefix && is300Weight && isPrimaryIntent,
    'placeholder:ring-offset-primary-400': isRingOffsetPrefix && is400Weight && isPrimaryIntent,
    'placeholder:ring-offset-primary-500': isRingOffsetPrefix && is500Weight && isPrimaryIntent,
    'placeholder:ring-offset-primary-600': isRingOffsetPrefix && is600Weight && isPrimaryIntent,
    'placeholder:ring-offset-primary-700': isRingOffsetPrefix && is700Weight && isPrimaryIntent,
    'placeholder:ring-offset-primary-800': isRingOffsetPrefix && is800Weight && isPrimaryIntent,
    'placeholder:ring-offset-primary-900': isRingOffsetPrefix && is900Weight && isPrimaryIntent,
    // info
    'placeholder:ring-offset-info-50': isRingOffsetPrefix && is50Weight && isInfoIntent,
    'placeholder:ring-offset-info-100': isRingOffsetPrefix && is100Weight && isInfoIntent,
    'placeholder:ring-offset-info-200': isRingOffsetPrefix && is200Weight && isInfoIntent,
    'placeholder:ring-offset-info-300': isRingOffsetPrefix && is300Weight && isInfoIntent,
    'placeholder:ring-offset-info-400': isRingOffsetPrefix && is400Weight && isInfoIntent,
    'placeholder:ring-offset-info-500': isRingOffsetPrefix && is500Weight && isInfoIntent,
    'placeholder:ring-offset-info-600': isRingOffsetPrefix && is600Weight && isInfoIntent,
    'placeholder:ring-offset-info-700': isRingOffsetPrefix && is700Weight && isInfoIntent,
    'placeholder:ring-offset-info-800': isRingOffsetPrefix && is800Weight && isInfoIntent,
    'placeholder:ring-offset-info-900': isRingOffsetPrefix && is900Weight && isInfoIntent,
    // success
    'placeholder:ring-offset-success-50': isRingOffsetPrefix && is50Weight && isSuccessIntent,
    'placeholder:ring-offset-success-100': isRingOffsetPrefix && is100Weight && isSuccessIntent,
    'placeholder:ring-offset-success-200': isRingOffsetPrefix && is200Weight && isSuccessIntent,
    'placeholder:ring-offset-success-300': isRingOffsetPrefix && is300Weight && isSuccessIntent,
    'placeholder:ring-offset-success-400': isRingOffsetPrefix && is400Weight && isSuccessIntent,
    'placeholder:ring-offset-success-500': isRingOffsetPrefix && is500Weight && isSuccessIntent,
    'placeholder:ring-offset-success-600': isRingOffsetPrefix && is600Weight && isSuccessIntent,
    'placeholder:ring-offset-success-700': isRingOffsetPrefix && is700Weight && isSuccessIntent,
    'placeholder:ring-offset-success-800': isRingOffsetPrefix && is800Weight && isSuccessIntent,
    'placeholder:ring-offset-success-900': isRingOffsetPrefix && is900Weight && isSuccessIntent,
    // warning
    'placeholder:ring-offset-warning-50': isRingOffsetPrefix && is50Weight && isWarningIntent,
    'placeholder:ring-offset-warning-100': isRingOffsetPrefix && is100Weight && isWarningIntent,
    'placeholder:ring-offset-warning-200': isRingOffsetPrefix && is200Weight && isWarningIntent,
    'placeholder:ring-offset-warning-300': isRingOffsetPrefix && is300Weight && isWarningIntent,
    'placeholder:ring-offset-warning-400': isRingOffsetPrefix && is400Weight && isWarningIntent,
    'placeholder:ring-offset-warning-500': isRingOffsetPrefix && is500Weight && isWarningIntent,
    'placeholder:ring-offset-warning-600': isRingOffsetPrefix && is600Weight && isWarningIntent,
    'placeholder:ring-offset-warning-700': isRingOffsetPrefix && is700Weight && isWarningIntent,
    'placeholder:ring-offset-warning-800': isRingOffsetPrefix && is800Weight && isWarningIntent,
    'placeholder:ring-offset-warning-900': isRingOffsetPrefix && is900Weight && isWarningIntent,
    // danger
    'placeholder:ring-offset-danger-50': isRingOffsetPrefix && is50Weight && isDangerIntent,
    'placeholder:ring-offset-danger-100': isRingOffsetPrefix && is100Weight && isDangerIntent,
    'placeholder:ring-offset-danger-200': isRingOffsetPrefix && is200Weight && isDangerIntent,
    'placeholder:ring-offset-danger-300': isRingOffsetPrefix && is300Weight && isDangerIntent,
    'placeholder:ring-offset-danger-400': isRingOffsetPrefix && is400Weight && isDangerIntent,
    'placeholder:ring-offset-danger-500': isRingOffsetPrefix && is500Weight && isDangerIntent,
    'placeholder:ring-offset-danger-600': isRingOffsetPrefix && is600Weight && isDangerIntent,
    'placeholder:ring-offset-danger-700': isRingOffsetPrefix && is700Weight && isDangerIntent,
    'placeholder:ring-offset-danger-800': isRingOffsetPrefix && is800Weight && isDangerIntent,
    'placeholder:ring-offset-danger-900': isRingOffsetPrefix && is900Weight && isDangerIntent,
    /**
     * @name fill
     */
    // light
    'placeholder:fill-light-50': isFillPrefix && is50Weight && isLightIntent,
    'placeholder:fill-light-100': isFillPrefix && is100Weight && isLightIntent,
    'placeholder:fill-light-200': isFillPrefix && is200Weight && isLightIntent,
    'placeholder:fill-light-300': isFillPrefix && is300Weight && isLightIntent,
    'placeholder:fill-light-400': isFillPrefix && is400Weight && isLightIntent,
    'placeholder:fill-light-500': isFillPrefix && is500Weight && isLightIntent,
    'placeholder:fill-light-600': isFillPrefix && is600Weight && isLightIntent,
    'placeholder:fill-light-700': isFillPrefix && is700Weight && isLightIntent,
    'placeholder:fill-light-800': isFillPrefix && is800Weight && isLightIntent,
    'placeholder:fill-light-900': isFillPrefix && is900Weight && isLightIntent,
    // dark
    'placeholder:fill-dark-50': isFillPrefix && is50Weight && isDarkIntent,
    'placeholder:fill-dark-100': isFillPrefix && is100Weight && isDarkIntent,
    'placeholder:fill-dark-200': isFillPrefix && is200Weight && isDarkIntent,
    'placeholder:fill-dark-300': isFillPrefix && is300Weight && isDarkIntent,
    'placeholder:fill-dark-400': isFillPrefix && is400Weight && isDarkIntent,
    'placeholder:fill-dark-500': isFillPrefix && is500Weight && isDarkIntent,
    'placeholder:fill-dark-600': isFillPrefix && is600Weight && isDarkIntent,
    'placeholder:fill-dark-700': isFillPrefix && is700Weight && isDarkIntent,
    'placeholder:fill-dark-800': isFillPrefix && is800Weight && isDarkIntent,
    'placeholder:fill-dark-900': isFillPrefix && is900Weight && isDarkIntent,
    // neutral
    'placeholder:fill-neutral-50': isFillPrefix && is50Weight && isNeutralIntent,
    'placeholder:fill-neutral-100': isFillPrefix && is100Weight && isNeutralIntent,
    'placeholder:fill-neutral-200': isFillPrefix && is200Weight && isNeutralIntent,
    'placeholder:fill-neutral-300': isFillPrefix && is300Weight && isNeutralIntent,
    'placeholder:fill-neutral-400': isFillPrefix && is400Weight && isNeutralIntent,
    'placeholder:fill-neutral-500': isFillPrefix && is500Weight && isNeutralIntent,
    'placeholder:fill-neutral-600': isFillPrefix && is600Weight && isNeutralIntent,
    'placeholder:fill-neutral-700': isFillPrefix && is700Weight && isNeutralIntent,
    'placeholder:fill-neutral-800': isFillPrefix && is800Weight && isNeutralIntent,
    'placeholder:fill-neutral-900': isFillPrefix && is900Weight && isNeutralIntent,
    // primary
    'placeholder:fill-primary-50': isFillPrefix && is50Weight && isPrimaryIntent,
    'placeholder:fill-primary-100': isFillPrefix && is100Weight && isPrimaryIntent,
    'placeholder:fill-primary-200': isFillPrefix && is200Weight && isPrimaryIntent,
    'placeholder:fill-primary-300': isFillPrefix && is300Weight && isPrimaryIntent,
    'placeholder:fill-primary-400': isFillPrefix && is400Weight && isPrimaryIntent,
    'placeholder:fill-primary-500': isFillPrefix && is500Weight && isPrimaryIntent,
    'placeholder:fill-primary-600': isFillPrefix && is600Weight && isPrimaryIntent,
    'placeholder:fill-primary-700': isFillPrefix && is700Weight && isPrimaryIntent,
    'placeholder:fill-primary-800': isFillPrefix && is800Weight && isPrimaryIntent,
    'placeholder:fill-primary-900': isFillPrefix && is900Weight && isPrimaryIntent,
    // info
    'placeholder:fill-info-50': isFillPrefix && is50Weight && isInfoIntent,
    'placeholder:fill-info-100': isFillPrefix && is100Weight && isInfoIntent,
    'placeholder:fill-info-200': isFillPrefix && is200Weight && isInfoIntent,
    'placeholder:fill-info-300': isFillPrefix && is300Weight && isInfoIntent,
    'placeholder:fill-info-400': isFillPrefix && is400Weight && isInfoIntent,
    'placeholder:fill-info-500': isFillPrefix && is500Weight && isInfoIntent,
    'placeholder:fill-info-600': isFillPrefix && is600Weight && isInfoIntent,
    'placeholder:fill-info-700': isFillPrefix && is700Weight && isInfoIntent,
    'placeholder:fill-info-800': isFillPrefix && is800Weight && isInfoIntent,
    'placeholder:fill-info-900': isFillPrefix && is900Weight && isInfoIntent,
    // success
    'placeholder:fill-success-50': isFillPrefix && is50Weight && isSuccessIntent,
    'placeholder:fill-success-100': isFillPrefix && is100Weight && isSuccessIntent,
    'placeholder:fill-success-200': isFillPrefix && is200Weight && isSuccessIntent,
    'placeholder:fill-success-300': isFillPrefix && is300Weight && isSuccessIntent,
    'placeholder:fill-success-400': isFillPrefix && is400Weight && isSuccessIntent,
    'placeholder:fill-success-500': isFillPrefix && is500Weight && isSuccessIntent,
    'placeholder:fill-success-600': isFillPrefix && is600Weight && isSuccessIntent,
    'placeholder:fill-success-700': isFillPrefix && is700Weight && isSuccessIntent,
    'placeholder:fill-success-800': isFillPrefix && is800Weight && isSuccessIntent,
    'placeholder:fill-success-900': isFillPrefix && is900Weight && isSuccessIntent,
    // warning
    'placeholder:fill-warning-50': isFillPrefix && is50Weight && isWarningIntent,
    'placeholder:fill-warning-100': isFillPrefix && is100Weight && isWarningIntent,
    'placeholder:fill-warning-200': isFillPrefix && is200Weight && isWarningIntent,
    'placeholder:fill-warning-300': isFillPrefix && is300Weight && isWarningIntent,
    'placeholder:fill-warning-400': isFillPrefix && is400Weight && isWarningIntent,
    'placeholder:fill-warning-500': isFillPrefix && is500Weight && isWarningIntent,
    'placeholder:fill-warning-600': isFillPrefix && is600Weight && isWarningIntent,
    'placeholder:fill-warning-700': isFillPrefix && is700Weight && isWarningIntent,
    'placeholder:fill-warning-800': isFillPrefix && is800Weight && isWarningIntent,
    'placeholder:fill-warning-900': isFillPrefix && is900Weight && isWarningIntent,
    // danger
    'placeholder:fill-danger-50': isFillPrefix && is50Weight && isDangerIntent,
    'placeholder:fill-danger-100': isFillPrefix && is100Weight && isDangerIntent,
    'placeholder:fill-danger-200': isFillPrefix && is200Weight && isDangerIntent,
    'placeholder:fill-danger-300': isFillPrefix && is300Weight && isDangerIntent,
    'placeholder:fill-danger-400': isFillPrefix && is400Weight && isDangerIntent,
    'placeholder:fill-danger-500': isFillPrefix && is500Weight && isDangerIntent,
    'placeholder:fill-danger-600': isFillPrefix && is600Weight && isDangerIntent,
    'placeholder:fill-danger-700': isFillPrefix && is700Weight && isDangerIntent,
    'placeholder:fill-danger-800': isFillPrefix && is800Weight && isDangerIntent,
    'placeholder:fill-danger-900': isFillPrefix && is900Weight && isDangerIntent,
  });
};
